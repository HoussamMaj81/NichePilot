# ── Android Automation ────────────────────────────────────────────────────
# Controls the TikTok app on your Android phone over USB via ADB
uiautomator2>=2.16.0

# ── Computer Vision & OCR ─────────────────────────────────────────────────
# Screenshot processing and on-screen text extraction
opencv-python>=4.8.0
easyocr>=1.7.0

# ── Machine Learning ──────────────────────────────────────────────────────
# TF-IDF vectorizer + Logistic Regression pipeline for niche classification
scikit-learn>=1.4.0
pandas>=2.0.0
joblib>=1.3.0

# ── AI Labeling ───────────────────────────────────────────────────────────
# Ollama Python client — used by labelaizer.py to call a local LLM
# Requires Ollama to be installed separately: https://ollama.com
ollama>=0.1.0
